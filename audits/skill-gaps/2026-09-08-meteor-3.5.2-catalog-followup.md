# Meteor 3.5.2 catalog drift follow-up

## Run context

```text
Agent-skills target ref: origin/main, fetched again on 2026-09-08
Agent-skills revision: 8b8047c0d78274ac1f6fa297f9d094b2239b4121; clean detached audit worktree
Meteor remote: https://github.com/meteor/meteor.git
Meteor branch: devel
Meteor revision: 7cb8141509a6e70972767f1106a2c708b5856efb; only unrelated untracked artifacts/
Meteor release context: Meteor 3.5.2; release/METEOR@3.5.2 at 4b13d6bb52c23f7bbb5df45d24af5a88bfd680f0
Audit mode: incremental catalog drift review
Comparison: agent-skills b458718f1ecfa5d7885f2de25a64379e770d1ef1..8b8047c0d78274ac1f6fa297f9d094b2239b4121; Meteor unchanged
Previous audit report: audits/skill-gaps/2026-09-08-meteor-3.5.2.md, preserved in the same uncommitted maintenance preparation
Previous Meteor revision: 7cb8141509a6e70972767f1106a2c708b5856efb
```

The earlier report records the original clean catalog snapshot. This follow-up
records a remote change discovered during final verification; neither report
claims the uncommitted maintenance has already been published or merged.

## Source coverage

Catalog [PR #6](https://github.com/meteor/agent-skills/pull/6) added
`meteor-cli-installation`, its references and nineteen cases, updated bundle
membership/catalog output, and added one near-miss case each to debugging and
Meteor 3 migration. The audit instructions, manifests, dependency lockfile and
the other distributable instructions are unchanged by the merge.

The new skill and references were read from a clean worktree at the new SHA.
Release-relevant claims were compared with the same Meteor revision:

- `v3-docs/docs/about/install.md`: npm installer, host prerequisites, PATH,
  supported shell alternative, cache recovery and uninstall.
- `npm-packages/meteor-installer/config.js`: default release 3.5.2, platform
  installation directory, environment override and PATH setup control.
- `tools/cli/commands.js`, reset command: default/custom local state and the
  database-preserving default. Versioned 3.0 and 3.4 source also preserves the
  default local database; custom-directory cleanup has its separate boundary.
- The existing `migrate-to-meteor-3` installer/reset guidance and the prepared
  Rspack targeted-cache recovery, for routing and state ownership.

Historical forum/issue examples in the new reinstall guide remain labeled
supporting context. They were not re-audited as current release requirements.

## Skill claim matrix

| Skill and surface | Changed claim / context | Evidence and classification | Evaluation |
|---|---|---|---|
| CLI entrypoint, installation/PATH reference | npm installer and app release are separate; a missing CLI has a distinct installation/PATH workflow. | Current install docs and installer config, Covered | CLI 1-4, 7, 9, 16 |
| CLI releases/reset reference | Meteor 3 reset preserves local Mongo by default; full uninstall is user-wide; custom local-directory reset is version-sensitive. | CLI source and versioned source, Covered | CLI 5, 6, 11, 18 |
| CLI reinstall decision reference | Project-local failure, shared metadata, host/download failure and user-wide corruption require different recovery scope. | Current installation docs plus explicitly labeled historical examples, Covered for release interaction | CLI 8, 10, 12-19 |
| Debugging evaluation file | Missing executable/PATH failure routes to CLI installation rather than application changes. | New CLI scope, Covered | Upstream debugging 19 |
| Meteor 3 migration evaluation file | A fresh workstation without an app routes to CLI installation. | New CLI scope, Covered | Upstream migration 28 |
| Prepared Rspack troubleshooting | Clear the identified Rspack cache before considering a broader project reset; preserve local Mongo. | 3.5.2 Rspack docs and CLI reset source, Covered | Prepared migration 21, 22 |

No additional release-driven guidance gap was confirmed in the newly merged
CLI skill. It increases the current catalog to fifteen skills; this task does
not claim authorship of that upstream addition.

## Maintenance handoff

Continue the original authorized F01-F08 changes against the new base. The
preparation branch was fast-forwarded and its uncommitted edits restored.
Resolve the append conflict by preserving upstream debugging case 19 and
renumbering this task's four new debugging cases to 20-23. Their prompt bodies
and acceptance conditions are unchanged, so the recorded behavioral outcomes
carry forward under those new numbers.

Regenerate the catalog, repeat repository validation/tests after integration,
and build/inspect all fifteen ZIPs. Keep the original audit and this follow-up
outside distribution artifacts. Preserve the new CLI skill and its bundle
membership without modifying its scope or introducing a sixteenth skill.

The explicit Claude plugin manifest passes normal validation with the existing
root `CLAUDE.md` warning; strict mode treats that warning as an error. The
directory-level strict command validates the marketplace and passes. Record
this distinction in release preparation; do not claim strict plugin validation
passed or remove the repository's maintainer context as part of this task.
